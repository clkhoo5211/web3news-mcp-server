{
  "name": "web3news-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for fetching RSS feeds, deployed on Vercel",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build skipped - Vercel handles TypeScript compilation automatically'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "html-to-text": "^9.0.1"
  },
  "devDependencies": {
    "@types/html-to-text": "^9.0.4",
    "@types/node": "^20.0.0",
    "typescript": "^5.7.2",
    "vercel": "^48.0.0"
  }
}
